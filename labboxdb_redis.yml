---
apiVersion: v1
kind: Service
metadata:
  name: labboxdb-redis
spec:
  selector:
    name: labboxdb-redis
  ports:
  - port: 6379
    targetPort: 6379
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: labboxdb-redis
spec:
  selector:
    matchLabels:
      name: labboxdb-redis
  replicas: 1
  template:
    metadata:
      labels:
        name: labboxdb-redis
        app: labbox
    spec:
      containers:
      - name: labboxdb-redis
        image: redis:5
        ports:
        - containerPort: 6379
          protocol: TCP
